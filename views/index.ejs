<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Online Examination Portal</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap');

        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh; 
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            background-color: #e0f7fa; /* Light cyan background */
            color: #333;
        }
        .container {
            position: relative; 
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            background-color: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(6px);
            border-radius: 12px;
            padding: 40px 30px;
            text-align: center;
            max-width: 700px;
            z-index: 1; 
        }
        .container h1 {
            font-size: 2.5em;
            margin-bottom: 20px;
            color: #00796b; /* Teal color */
            font-weight: 600;
        }

        .container p {
            font-size: 1.2em;
            margin-bottom: 30px;
            color: #555;
        }

        .buttons {
            display: flex;
            gap: 25px;
            justify-content: center;
            flex-wrap: wrap; /* Ensures responsiveness */
        }

        .button {
            background-color: #00796b; /* Teal color */
            color: #fff;
            padding: 14px 30px;
            text-decoration: none;
            border-radius: 6px;
            font-size: 1.1em;
            transition: background-color 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
        }

        .button:hover {
            background-color: #004d40; /* Darker teal color */
            transform: translateY(-4px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
        }

        .background-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1; 
            filter: brightness(75%);
        }
    </style>
</head>
<body>
    <img src="inde_bg.jpg" alt="Background Image" class="background-image">
    <div class="container">
        <h1>Welcome to Our Online Examination Portal</h1>
        <p>Your one-stop solution for managing and taking exams online. Whether you're an educator or a student, we have the tools you need to succeed.</p>
        <div class="buttons">
            <a href="/Educator" class="button">I'm an Educator</a>
            <a href="/Student" class="button">I'm a Student</a>
        </div>
    </div>
</body>
</html>
