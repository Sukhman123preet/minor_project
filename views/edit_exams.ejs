<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Exam</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #2c3e50; /* Dark background */
            color: #e0e0e0; /* Light text color */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background-color: #10121a; /* Darker background for form container */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            width: 100%;
            max-width: 500px;
        }

        h1 {
            margin-bottom: 20px;
            color: #e0e0e0;
            text-align: center;
            font-size: 24px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: #e0e0e0;
        }

        input[type="text"],
        input[type="number"],
        input[type="date"],
        textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #3b4e63; /* Subtle border color */
            border-radius: 5px;
            background-color: #2c3e50; /* Dark input background */
            color: #e0e0e0; /* Light text color in inputs */
            font-size: 14px;
        }

        textarea {
            resize: vertical; /* Allow vertical resizing of the textarea */
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: #1abc9c; /* Button color */
            border: none;
            border-radius: 5px;
            color: #ffffff; /* Button text color */
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #16a085; /* Darker button color on hover */
        }

        .btn {
            display: inline-block;
            margin-top: 10px;
            padding: 10px 15px;
            background-color: #3498db; /* Back button color */
            border-radius: 5px;
            color: #ffffff;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #2980b9; /* Darker back button color on hover */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Edit Exam: <%= exam.title %></h1>
        <form action="/exams/update/<%= exam._id %>" method="POST">
            <div class="form-group">
                <label for="title">Exam Title:</label>
                <input type="text" id="title" name="title" value="<%= exam.title %>" required>
            </div>

            <div class="form-group">
                <label for="description">Description:</label>
                <textarea id="description" name="description" required><%= exam.description %></textarea>
            </div>

            <div class="form-group">
                <label for="date">Exam Date:</label>
                <input type="date" id="date" name="date" value="<%= exam.date.toISOString().split('T')[0] %>" required>
            </div>

            <div class="form-group">
                <label for="duration">Duration (in minutes):</label>
                <input type="number" id="duration" name="duration" value="<%= exam.duration %>" required>
            </div>

            <div class="form-group">
                <label for="totalMarks">Total Marks:</label>
                <input type="number" id="totalMarks" name="totalMarks" value="<%= exam.totalMarks %>" required>
            </div>

            <div class="form-group">
                <label for="passingMarks">Passing Marks:</label>
                <input type="number" id="passingMarks" name="passingMarks" value="<%= exam.passingMarks %>" required>
            </div>

            <div class="form-group">
                <button type="submit">Update Exam</button>
            </div>
        </form>

        <div class="form-group">
            <a href="/exams" class="btn">Back to Exams</a>
        </div>
    </div>
</body>
</html>
